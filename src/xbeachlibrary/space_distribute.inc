! This file is generated by 'scripts/generate.py' using
!    /src/xbeachlibrary/variables.def
!    /src/xbeachlibrary/params.def
!    src/xbeachlibray/templates/space_distribute.mako
! Advice: do not edit this file, but above mentioned files.

    logical, optional, intent(in)       :: toall
    integer src,comm
    logical iamsrc
    src    = xmpi_master
    comm   = xmpi_comm
    iamsrc = xmaster
    if(present(toall)) then
      if(toall) then
        src    = xmpi_imaster
        comm   = xmpi_ocomm
        iamsrc = xomaster
      endif
    endif
      if (xomaster) return
!directions for vi vim: filetype=fortran : syntax=fortran

! vim: filetype=fortran
